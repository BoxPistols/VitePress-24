{
  "license": "MIT",
  "devDependencies": {
    "vitepress": "^1.0.0-rc.36"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "prettier": "prettier --write \"./**/*.md\"",
    "lint": "markdownlint \"./**/*.md\" --ignore node_modules --ignore docs/.vitepress --ignore docs/.vitepress/dist"
  },
  "prettier": {
    "plugins": [
      "prettier-plugin-md-nocjsp"
    ]
  },
  "dependencies": {
    "markdownlint": "^0.33.0",
    "prettier": "^3.2.2",
    "prettier-plugin-md-nocjsp": "^1.5.1"
  },
  "browserslist": [
    "defaults",
    "not ie <= 11",
    "not op_mini all"
  ]
}